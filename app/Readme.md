```
ToDo
    Modelo >> clase tarea >> tabla tarea
        -taskModel.php >> definir atributos
    controller>> taskcontroller - metodos a implementar
        -crear tarea
        -delete tarea
        -modificar tarea
        -listar tarea
        -completar tarea (cambio de estado)
    Vistas >> 
        -create.pthml
        -delete.pthml
        -modificar.pthml
        -listar.pthml >> tambien contempla la funcionalidad de >> completar tarea (cambio de estado)
``` 